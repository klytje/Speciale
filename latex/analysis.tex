\section{Analysis}
\note{Remember to generate all figures with the full dataset to replace the placeholders.} \\

After having passed through the entire \AUSAlib{} pipeline, the data is finally ready for the actual analysis. Before we perform any further calculations with the data, we first need to impose some cuts on it. This is necessary as, although we only have multiplicity $3$ events, there is no guarantee that the three particles actually originate from the same decay. \\

In total we would like to perform three cuts: one on the \TDC{} values, another on the energy, and a final one on the momentum. Unless stated otherwise, all figures from this section are made with only multiplicity $3$ data.

\subsection{TDC cut}
The general idea with this type of cut is that a given $\alpha$-particle should hit the front and backside of a detector within some small time interval. If this interval is too large, the two hits are probably not from the same particle, and the event should be discarded. \\

It turns out that this cut is not that simple to perform, however. There are two issues with the \TDC{} values which must first be addressed. The first is a matter of alignment: the \TDC{} values for the different strips are not necessarily aligned with each other. The second is a major issue which was discovered with the data for only the $0+$ measurement. We must first deal with the latter. 

\subsubsection{Repairing the data}
\begin{figure}
	\includegraphics[width=\linewidth]{figures/tdc_plot_before.pdf}
	\caption{The issue with the \TDC{} values. The values on the y-axis are the \TDC{} values for the front (\FT{}) and backside (\BT{}). The x-axis labels the four detectors used for the experiment. Note specifically the major difference between \BT{} and \FT{} for Det1 and Det2; the \FT{} peaks appear to be broken. }
	\label{fig: analysis: tdc before}
\end{figure}

To see the problem, look at figure \ref{fig: analysis: tdc before}. The figure contains all measured \TDC{} values by the detectors (x-axis), both for the front and backside (y-axis). Only the backside of the S3 detectors were hooked up to the \TDC{} converter, hence why the two bottom-left panels are empty. Two problems are clearly visible in this figure: one, we have too many peaks, and two, some of the peaks appear to be broken in half. We will focus on the latter problem first. \\

Such a clean split down the middle of a peak, with its two halves appearing at the very edges of the plot, looks like a classic overflow-problem, and indeed, it probably is. The right half of every broken peak stops at exactly $2^{16} = 65536$, which makes sense since the \note{something about the word size being equal to this}. However, things are not that simple: the left half of every broken peak starts at exactly $65126$; i.e. the peaks are only shifted by $410$, and does not overflow back to $0$, as one would expect. Upon a closer examination of all data sets, it appears as if this range, $\{65126, 65536\}$ are the \textit{only} possible values for the \TDC{} besides $0$ \note{check this}. Why this is the case is unclear, but since we are only interested in the \textit{differences} between \TDC{} values, we can choose to ignore the cause and simply fix the data. \\

\begin{figure}
	\includegraphics[width=\linewidth]{figures/tdc_plot_after.pdf}
	\caption{The corrected \TDC{} values. The issue seen on figure \ref{fig: analysis: tdc before} has now been corrected.}
	\label{fig: analysis: tdc after}
\end{figure}

Figure \ref{fig: analysis: tdc after} shows the data after it has been corrected. This reveals another piece of the puzzle: the two halves of the peaks does not match up exactly. It is, however, a very close match, and so, once again, we have chosen to simply accept the data as it is. \\

\begin{figure}
	\includegraphics[width=\linewidth]{figures/mul_compare.pdf}
	\caption{The second issue with the data. The multiple peaks are due to primary and secondary activations. See the text for a detailed discussion. }
	\label{fig: analysis: mul}
\end{figure}

With the second problem fixed, we now turn our attention to the first: we still have multiple significant peaks in essentially all our panels. The S3 detectors especially are problematic. To understand why this is the case, we must first discuss some of the finer details of the detectors. \\

A detector $A$ can be activated in two ways. The first case is when $A$ itself detects a hit, after which it will immediately send a signal to the \TDC{} converter. The second case is when another detector $B$ is hit, and sends an activation signal to $A$. Due to the additional length of wiring the signal must pass through from $B$ to $A$, the second case will in general be delayed compared to the first. Since the length of wiring may be different for signals from each detector $\{B, C, D,...\}$, this may result in a unique peak for each. I will refer to these two cases as primary and secondary activations, respectively. \\

These two types of activations are exactly what we see on figure \ref{fig: analysis: tdc after}, which we can realize by separating the data into different sets depending on the multiplicity. For multiplicity $1$ data (we are temporarily working with the data before these events were discarded), only primary activations can occur. For higher multiplicities, both primary and secondary activations should be present, with an increasing number of secondary compared to primary as the multiplicity increases. This is exactly what we see on figure \ref{fig: analysis: mul}, showing only the \BT{} peaks. The \FT{} peaks for the W1's acts identically. \note{maybe discuss why this makes sense - due to downscaling and position, we expect W1 to primary activate more than S3} \\

This explains all but a single extra peak, which can be seen on the second top panel from the left. Some further investigation reveals that this extra peaks consists almost entirely of low-energy events, as can be seen on figure \ref{fig: appendix: multiple peaks energy} in the appendix. With the cause of every peak identified, we can now solve the problem by simply shifting all of them on top of each other, as can be seen for the \BT{} peaks on figure \ref{fig: analysis: TDC: BT peaks shifted}. 

\begin{figure}[bt]
	\centering
	\begin{minipage}{.5\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/align_peaks_BT_simultaneous.pdf}
	\end{minipage}%
	\begin{minipage}{.5\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/align_peaks_BT_aligned.pdf}
	\end{minipage}
	\caption{Shifting the multiple \BT{} peaks on top of each other. The left panel shows the multi-Gaussian fit, which allows us to obtain the mean $\mu_i$ and standard deviation $\sigma_i$ for each peak. The $i$'th peak is then moved by shifting all bins within $3\sigma_i$ by $\mu_i-\mu_0$, thus stacking it on top of the first peak. The result of this process can be seen on the right panel.}
	\label{fig: analysis: TDC: BT peaks shifted}
\end{figure}

\subsubsection{Alignment}
Now that we have fixed the issue with multiple peaks, it is time to look at the other problem of aligning the \TDC{} values. For the remainder of this section, we are only interested in the \textit{difference} in \FT{} and \BT{} values, which is problematic for the two S3 detectors since they only measure the latter. We can get around this problem by looking only at multi-detector events, i.e. cases where the decay products were detected by at least two different detectors. As long as one of these two detectors are a W1, we can simply use its \FT{} value for the S3 as well. We can do this since this should simply be a constant offset from whatever the S3 would have measured by itself. \\

Now, to convince ourselves that it is actually necessary to align the \TDC{} values, take a look at the two left panels of figure \ref{fig: analysis: TDC}, which shows the difference in \FT{} and \BT{} values for the two detectors Det1 (top) and SU (bottom). As it is right now, it is impossible to fit the 1D projection with a Gaussian; hence the need for this alignment. \\

\begin{figure}[bt]
	\centering
	\begin{minipage}{.33\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/tdc_calibration_raw_Det1.pdf}
	\end{minipage}%
	\begin{minipage}{.33\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/tdc_calibration_centered_Det1.pdf}
	\end{minipage}%
	\begin{minipage}{.33\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/gauss_cut_before_Det1.pdf}
	\end{minipage}

	\begin{minipage}{.33\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/tdc_calibration_raw_SU.pdf}
	\end{minipage}%
	\begin{minipage}{.33\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/tdc_calibration_centered_SU.pdf}
	\end{minipage}%
	\begin{minipage}{.33\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/gauss_cut_before_SU.pdf}
	\end{minipage}
	\caption{The three main steps of the \TDC{} calibration. The top row shows the process for Det1, while the bottom row is for the SU. The left panels shows the raw data, neatly illustrating why it must be aligned. The middle panels shows the same data after each strip has been centered. The right panels shows the outcome of the minimization procedure, and also the results of the Gaussian fits. All events within the $3\sigma$ red lines will be kept.}
	\label{fig: analysis: TDC}
\end{figure}

Before we start modifying the data, we need to discuss what we are actually allowed to do. When a strip sends a signal to the \TDC{}, it must first travel through some length of wire, which takes some time $t$. Ideally these wires would all be the same length, or the \TDC{} would be calibrated to account for the differences, which would automatically solve this alignment problem. Since this is not the case, our goal is to determine the difference in wire length, $\Delta t$. We do this by minimizing the equation
\begin{align} \label{analysis: tdc: minimize}
	\sum_{hits} |(t_i + \Delta_i) - (t_j + \Delta_j)|
\end{align}
where $\{i, j\}$ run over the number of strips. We use the absolute difference instead of least squares since we expect outliers from random coincidences. \\

The first step is to extract the mean $dt$ for each strip, and use this to center them around $0$ - this operation amounts to applying an offset to each $\Delta t$. As can be seen on the middle panels of figure \ref{fig: analysis: TDC}, this already dramatically improves the alignment, but we can still do better. By now performing the minimization from (\ref{analysis: tdc: minimize}), we arrive at the two right panels of the same figure. We can finally perform a Gaussian fit to this and only keep events within $3\sigma$, which corresponds to the area enclosed by the red lines also shown on the two right panels. \\

With this cut applied, we are finally ready to move on to the energy and momentum cuts.

\subsection{Energy \& momentum cuts}
To help guide our choice of cutoff values, an obvious first step would be to plot the energy against the momentum in a 2D histogram. This can be seen in figure \ref{fig: analysis: energy momentum cuts}, which shows the raw data in three cases: before any cuts (left), after the \TDC{} cut (middle), and the energy/momentum cut (right). The cutoff values for energy and momentum were manually selected to preserve the central events around $E_{tot} = \SI{17.76}{MeV}$. \\

\begin{figure}[bt]
	\centering
	\begin{minipage}{.33\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/cuts_raw.pdf}
	\end{minipage}%
	\begin{minipage}{.33\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/cuts_tdc.pdf}
	\end{minipage}%
	\begin{minipage}{.33\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/cuts_Ep.pdf}
	\end{minipage}
	\caption{The effects of the three cuts. The left panel shows the raw data, without any cuts. The middle panel shows the same data after the \TDC{} cut has been applied. The right panel is a zoomed-in version of the middle panel, where the  vertical and horizontal lines illustrates the energy and momentum cut, respectively. Note the different column scale in each panel. }
	\label{fig: analysis: energy momentum cuts}
\end{figure}

With all our cuts imposed on the data, we can finally continue with the data analysis. 

\subsection{Dalitz plots}
The purpose of this thesis is to test the validity of the currently established models for the $0+$ state. Thus a nice opener to the analysis is to make a Dalitz plot of the data. This can be seen on the left panel of figure \ref{fig: analysis: dalitz: slice}, which reveals that another cut is necessary. The figure shows that a large concentration of events are located in a band near $y \simeq 1$, where almost the entire energy of the first decay is given to the $\alpha$-particle. These events thus corresponds to decays through the ground state of \ce{^8Be}. \note{more}. The right panel of the figure is only there to more clearly illustrate the structure in the remaining areas. \\

\begin{figure}[]
	\centering
	\begin{minipage}{.5\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/dalitz_slice.pdf}
	\end{minipage}%
	\begin{minipage}{.5\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/dalitz_slice_cut.pdf}
	\end{minipage}
	\caption{The cuts on the Dalitz plot. The left panel shows the data before the cut, with the red lines signifying the cutoffs. The right panel shows the same data after the cut, to more clearly show its structure. }
	\label{fig: analysis: dalitz: slice}
\end{figure}

Although all of the kinematic information is already stored in this slice, it is somewhat easier to see the structure in a standard Dalitz plot. We can reconstruct it by repeatedly mirroring the slice along one of its edges. By doing this, we obtain figure \ref{fig: analysis: dalitz: full}. The left panel shows the multiplicity three data, while the right panel shows the multiplicity two data. Although there are some minor deviations between the two panels, they both clearly show an equilateral triangle. \\

\begin{figure}[]
	\centering
	\begin{minipage}{.5\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/dalitz_true.pdf}
	\end{minipage}%
	\begin{minipage}{.5\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/dalitz_reconstructed.pdf}
	\end{minipage}
	\caption{The Dalitz plots for the data. The left panel shows the plot for our multiplicity three data, while the right shows it for the multiplicity two data. Note the large difference in the column scale. }
	\label{fig: analysis: dalitz: full}
\end{figure}

Our next goal is to figure out what we expected to see. Since Zemach's analysis does not impose any constraints upon the $0+$ resonance at all, we are left with performing simulations. As mentioned in section \ref{section: data reduction}, we have two options for these: \simX{} and \simta{}. Only the latter accounts for interference, and is thus our preferred choice. \\

The Monte-Carlo simulations are performed with \simta{}, after which they are put through \simX{}, which simulates interactions with the experimental setup. Then they are put through the entire \AUSAlib{} pipeline, after which we can make a Dalitz plot of them, like we did for the actual data. This results in the left panel of figure \ref{fig: analysis: dalitz: sim}. Something is clearly very wrong here: it does not at all look like our data. As a check, the same simulations were performed solely with \simX{}, which results in the right panel of the same figure. While this is somewhat better, it is still very different from our data.

\begin{figure}[]
	\centering
	\begin{minipage}{.5\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/dalitz_sim3a.pdf}
	\end{minipage}%
	\begin{minipage}{.5\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/dalitz_simX.pdf}
	\end{minipage}
	\caption{The Dalitz plots for the $0+$ simulations. The left panel shows the results of the \simta{} simulation, while the right shows it for the \simX{} simulation. For a discussion on the difference between the two tools, see section \note{ref}.}
	\label{fig: analysis: dalitz: sim}
\end{figure}

\subsubsection{Checking the simulation tools}
Since the simulations are so different from what we see in our data, it is worthwhile to verify that they actually work as expected. Thus for this small section, we will work with data from the $2-$ resonance instead. Using specifically the \simta{} tool on this resonance was a major part of M. Kuhlwein's thesis \cite{Kuhlwein}, a previous student at the same faculty. Replicating his best fit of the resonance with ${k = 0.186}$ and ${\delta = 0.69\cdot2\pi}$, we get figure \ref{fig: analysis: dalitz: 2-}. We can already by eye tell that this is a much better match. \\

\begin{figure}[]
	\centering
	\begin{minipage}{.5\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/2-/dalitz.pdf}
	\end{minipage}%
	\begin{minipage}{.5\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/2-/dalitz_sim3a.pdf}
	\end{minipage}
	\caption{The Dalitz plots for the $2-$ resonance. The left panel shows both the multiplicity two and three data, while the right shows the \simta{} simulation with $k=0.186$ and $\delta=0.69\cdot2\pi$. \note{normalize both? fix axes}}
	\label{fig: analysis: dalitz: 2-}
\end{figure}

Since it is somewhat hard to compare 2D histograms, it is common to look at their projections instead. A popular choice are the polar projections, along with the center-mass energies. If we plot these, we get the three panels shown on figure \ref{fig: analysis: dalitz: 2- projections}. Here the black lines represents the data, while the orange represents the simulation. Although there are some deviations, especially for the radial projection $\rho$, this is overall a great fit to the data. With an assurance that the simulations are not wrong, we are instead left wondering what else could be the problem. 

\begin{figure}[bt]
	\centering
	\begin{minipage}{.33\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/2-/E_cm.pdf}
	\end{minipage}%
	\begin{minipage}{.33\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/2-/rho.pdf}
	\end{minipage}%
	\begin{minipage}{.33\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/2-/phi.pdf}
	\end{minipage}
	\caption{The projections of the simulated $2-$ Dalitz plot with ${k=0.186}$ and ${\delta = 0.69\cdot2\pi}$. The black lines represents the data, while the orange lines are the simulations. The simulations nicely matches the data in all three projections, in line with the conclusion reached by Kuhlwein \cite{Kuhlwein}. }
	\label{fig: analysis: dalitz: 2- projections}
\end{figure}

\subsubsection{Checking the reduced width amplitude}
A solution could be that the reduced width amplitude used for the excited state \ce{^8Be*} is wrong. The value used for the previous simulation is $\gamma = \SI{32.787}{\sqrt{keV}}$ \note{where from?}. We can simultaneously use this as an additional check on our simulation tools: as the width becomes smaller, interference effects becomes less relevant, and so the two simulations should converge towards each other for low $\gamma$. Thus a conversion tool based on eq (\ref{theory: r-matrix: Gamma gamma}) was made to convert between $\gamma$ and $\Gamma$, allowing us to compare the two simulation tools. \\

As we discussed in the theory, section \ref{section: theory}, in a consecutive three-body breakup, the angle between the primary and secondary \note{ensure these terms are defined} particles are correlated. We also derived the correlation function $W(\beta)$ (eq (\ref{theory: angular correlations: W}) for an arbitrary state. This is an excellent method of comparing our simulations with the theory, which we have used on figure \ref{fig: analysis: ang compare sim3a 0} for \simta{}, and on figure \ref{fig: analysis: ang compare simX 0} for \simX{}. The x-labels for the \simta{} panels are the reduced width amplitude $\gamma$, while they are the corresponding $\Gamma$ on the \simX{} panels. The horizontal red band on the top panels shows the area projected down onto the x-axis in the bottom panels. On the bottom panels, the red line shows the correlation function $W(\beta)$, where $\theta = \beta + \pi$ is the angle from eq (\ref{theory: dalitz: theta}). The agreement between theory and simulation for \simta{} is generally quite good, except at the point of maximum interference. \simX{} is not quite as good, especially around the points of interference, and generally appears to be more smeared. The general shape is clearly seen in both figures, indicating that they work as expected. The reason they do not appear to converge may be because we need even smaller $\gamma$, which are not possible with these tools due to instabilities. For larger comparison values of $\gamma$, see figure \ref{fig: appendix: ang compare all} in the appendix. \\

\begin{figure}
	\includegraphics[width=\linewidth]{figures/ang_compare_0_sim3a.pdf}
	\caption{}
	\label{fig: analysis: ang compare sim3a 0}
	
	\includegraphics[width=\linewidth]{figures/ang_compare_0_simX.pdf}
	\caption{}
	\label{fig: analysis: ang compare simX 0}
\end{figure}

From figure \ref{fig: analysis: ang compare sim3a 0} for \simta{}, we see that both simulation and theory predicts that the bands vanish near the points of interference. Yet this is not what we see in our data on figure \ref{fig: analysis: dalitz: full}. One explanation for this is that our data is dominated by interference from other nearby states. To check this, we can try determining which correlation functions \textit{do} describe our data. 

\subsubsection{Checking the angular correlations}
As a first step, we can try overlaying all correlation functions up to and including the $J = 3$ states on top of the distribution from the data. This results in the rather messy figure \ref{fig: analysis: ang cor fit}. 

\begin{figure}
	\centering
	\begin{minipage}{.5\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/ang_cor_fit_true.pdf}
	\end{minipage}%
	\begin{minipage}{.5\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/ang_cor_fit_reconstructed.pdf}
	\end{minipage}
	\caption{}
	\label{fig: analysis: ang cor fit}
\end{figure}

\begin{figure}
	\centering
	\begin{minipage}{.5\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/ang_cor_fitted_true.pdf}
	\end{minipage}%
	\begin{minipage}{.5\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/ang_cor_fitted_reconstructed.pdf}
	\end{minipage}
	\caption{}
	\label{fig: analysis: ang cor fitted}
\end{figure}

\subsection{Some title}
\subsubsection{Verifying the branching ratio}