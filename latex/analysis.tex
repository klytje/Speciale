\section{Analysis}
After having passed through the entire \AUSAlib{} pipeline, the data is finally ready for the actual analysis. Before we perform any further calculations with the data, we first need to impose some cuts on it. The reasoning behind this is two-fold: one, to weed out any proton detections, and two, to ensure that any multi-coincidence $\alpha$ particle hits actually originate from the same decay event. \\

In total we would like to perform three cuts: one on the \TDC{} values, another on the energy, and a final one on the momentum. Unless stated otherwise, all figures from this section are made with only multiplicity $3$ data. We will also often use the notation $J^{\pi}_l$ to specify specific transitions. 

\subsection{TDC cut}
The general idea with this type of cut is that a given $\alpha$-particle should hit the front and backside of a detector within some small time interval. If this interval is too large, the two hits are probably not from the same particle, and the event should be discarded. \\

It turns out that this cut is not that simple to perform, however. There are two issues with the \TDC{} values which must first be addressed. The first is a matter of alignment: the \TDC{} values for the different strips are not necessarily aligned with each other. The second is a major issue which was discovered with the data for only the $0^+$ measurement. We must first deal with the latter. 

\subsubsection{Repairing the data}
\begin{figure}
	\includegraphics[width=\linewidth]{figures/0+/true_events/tdc_plot_before.pdf}
	\caption{The issue with the \TDC{} values. The values on the y-axis are the \TDC{} values for the front (\FT{}) and backside (\BT{}). The x-axis labels the four detectors used for the experiment. Note specifically the major difference between \BT{} and \FT{} for Det1 and Det2; the \FT{} peaks appear to be broken. }
	\label{fig: analysis: tdc before}
\end{figure}

To see the problem, look at figure \ref{fig: analysis: tdc before}. The figure contains all measured \TDC{} values by the detectors (x-axis), both for the front and backside (y-axis). Only the backside of the S3 detectors were hooked up to the \TDC{} converter, hence why the two bottom-left panels are empty. Two problems are clearly visible in this figure: one, we have too many peaks, and two, some of the peaks appear to be broken in half. We will focus on the latter problem first. \\

Such a clean split down the middle of a peak, with its two halves appearing at the very edges of the plot, looks like a classic overflow-problem, and indeed, it probably is. The right half of every broken peak stops at exactly $2^{16} = 65536$, which is coincidentally the same number of bits used for the \TDC{} data word size. This clearly indicates that the problem is a simple overflow. However, things are not that simple: the left half of every broken peak starts at exactly $65126$; i.e. the peaks are only shifted by $410$, and does not overflow back to $0$, as we would expect. Upon a closer examination of all data sets, it appears as if this range, $\{65126, 65536\}$ contains the \textit{only} possible values for the \TDC{} besides $0$. Why this is the case is unclear, but since we are only interested in the \textit{differences} between \TDC{} values, we can choose to ignore the cause and simply fix the data. \\

\begin{figure}
	\includegraphics[width=\linewidth]{figures/0+/true_events/tdc_plot_after.pdf}
	\caption{The corrected \TDC{} values. The issue seen on figure \ref{fig: analysis: tdc before} has now been corrected.}
	\label{fig: analysis: tdc after}
\end{figure}

Figure \ref{fig: analysis: tdc after} shows the data after it has been corrected. This reveals another piece of the puzzle: the two halves of the peaks do not match up exactly. It is, however, a very close match, and so, once again, we have chosen to simply accept the data as it is. \\

\begin{figure}[tb]
	\includegraphics[width=\linewidth]{figures/0+/depend_mul.pdf}
	\caption{The second issue with the data. The multiple peaks are due to primary and secondary activations. See the text for a detailed discussion. }
	\label{fig: analysis: mul}
\end{figure}

With the second problem fixed, we now turn our attention to the first: we still have multiple significant peaks in essentially all our panels. The S3 detectors especially are problematic. To understand why this is the case, we must first discuss some of the finer details of the detectors. \\

A detector $A$ can be activated in two ways. The first case is when $A$ itself detects a hit, after which it will immediately send a signal to the \TDC{} converter. The second case is when another detector $B$ is hit, and sends an activation signal to $A$. Due to the additional length of wiring the signal must pass through from $B$ to $A$, the second case will in general be delayed compared to the first. Since the length of wiring may be different for signals from each detector $\{B, C, D,...\}$, this may result in a unique peak for each. I will refer to these two cases as primary and secondary activations, respectively. \\

These two types of activations are exactly what we see on figure \ref{fig: analysis: tdc after}, which we can realize by separating the data into different sets depending on the multiplicity. For multiplicity $1$ data (we are temporarily working with the data before these events were discarded), only primary activations can occur. For higher multiplicities, both primary and secondary activations should be present, with an increasing number of secondary compared to primary as the multiplicity increases. This is exactly what we see on figure \ref{fig: analysis: mul}, showing only the \BT{} peaks. The \FT{} peaks for the W1's acts identically. This interpretation also explains why the secondary peaks for the S3 detectors are relatively larger than those for the W1s: since the upstream SU only detects backscattered particles, and the downstream SD is heavily downscaled, we would expect the W1s to be mainly primary activated, which is exactly what we see. \\

This explains all but a single extra peak, which can be seen on the second top panel from the left. Some further investigation reveals that this extra peaks consists almost entirely of low-energy events, as can be seen on figure \ref{fig: appendix: multiple peaks energy} in the appendix. With the cause of every peak identified, we can now solve the problem by simply shifting all of them on top of each other, as can be seen for the \BT{} peaks on figure \ref{fig: analysis: TDC: BT peaks shifted}. 

\begin{figure}[bt]
	\centering
	\begin{minipage}{.5\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/0+/true_events/align_peaks_BT_simultaneous.pdf}
	\end{minipage}%
	\begin{minipage}{.5\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/0+/true_events/align_peaks_BT_aligned.pdf}
	\end{minipage}
	\caption{Shifting the multiple \BT{} peaks on top of each other. The left panel shows the multi-Gaussian fit, which allows us to obtain the mean $\mu_i$ and standard deviation $\sigma_i$ for each peak. The $i$'th peak is then moved by shifting all bins within $3\sigma_i$ by $\mu_i-\mu_0$, thus stacking it on top of the first peak. The result of this process can be seen on the right panel.}
	\label{fig: analysis: TDC: BT peaks shifted}
\end{figure}

\subsubsection{Alignment}
Now that we have fixed the issue with multiple peaks, it is time to look at the other problem of aligning the \TDC{} values. For the remainder of this section, we are only interested in the \textit{difference} in \FT{} and \BT{} values, which is problematic for the two S3 detectors since they only measure the latter. We can get around this problem by looking only at their multi-detector events, i.e. cases where the decay products were detected by at least two different detectors. As long as one of these two detectors are a W1, we can simply use its \FT{} value for the S3 as well. We can do this since this should simply be a constant offset from whatever the S3 would have measured by itself. \\

Now, to convince ourselves that it is actually necessary to align the \TDC{} values, take a look at the two left panels of figure \ref{fig: analysis: TDC}, which shows the difference in \FT{} and \BT{} values for the two detectors Det1 (top) and SU (bottom). As it is right now, it is impossible to fit the 1D projection with a Gaussian; hence the need for this alignment. \\

\begin{figure}[bt]
	\centering
	\begin{minipage}{.33\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/0+/true_events/tdc_calibration_raw_Det1.pdf}
	\end{minipage}%
	\begin{minipage}{.33\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/0+/true_events/tdc_calibration_centered_Det1.pdf}
	\end{minipage}%
	\begin{minipage}{.33\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/0+/true_events/gauss_cut_before_Det1.pdf}
	\end{minipage}

	\begin{minipage}{.33\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/0+/true_events/tdc_calibration_raw_SU.pdf}
	\end{minipage}%
	\begin{minipage}{.33\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/0+/true_events/tdc_calibration_centered_SU.pdf}
	\end{minipage}%
	\begin{minipage}{.33\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/0+/true_events/gauss_cut_before_SU.pdf}
	\end{minipage}
	\caption{The three main steps of the \TDC{} calibration. The top row shows the process for Det1, while the bottom row is for the SU. The left panels shows the raw data, neatly illustrating why it must be aligned. The middle panels shows the same data after each strip has been centered. The right panels shows the outcome of the minimization procedure, and also the results of the Gaussian fits. All events within the $3\sigma$ red lines will be kept.}
	\label{fig: analysis: TDC}
\end{figure}

Before we start modifying the data, we need to discuss what we are actually allowed to do. When a strip sends a signal to the \TDC{}, it must first travel through some length of wire, which takes some time $t$. Ideally these wires would all be the same length, or the \TDC{} would be calibrated to account for the differences, which would automatically solve this alignment problem. Since this is not the case, our goal is to determine the difference in wire length, $\Delta t$. We do this by minimizing the equation
\begin{align} \label{analysis: tdc: minimize}
	\sum_{\text{hits}} |(t_i + \Delta_i) - (t_j + \Delta_j)|
\end{align}
where $\{i, j\}$ run over the number of strips. We use the absolute difference instead of least squares since we expect outliers from random coincidences. \\

The first step is to extract the mean $dt$ for each strip, and use this to center them around $0$ - this operation amounts to applying an offset to each $\Delta t$. As can be seen on the middle panels of figure \ref{fig: analysis: TDC}, this already dramatically improves the alignment, but we can still do better. By now performing the minimization from (\ref{analysis: tdc: minimize}), we arrive at the two right panels of the same figure. We can finally perform a Gaussian fit to this and only keep events within $3\sigma$, which corresponds to the area enclosed by the red lines also shown on the two right panels. \\

With this cut applied, we are finally ready to move on to the energy and momentum cuts.

\subsection{Energy \& momentum cuts}
To help guide our choice of cutoff values, an obvious first step would be to plot the energy against the momentum in a 2D histogram. This can be seen in figure \ref{fig: analysis: energy momentum cuts}, which shows the raw data in three cases: before any cuts (left), after the \TDC{} cut (middle), and the energy/momentum cut (right). The cutoff values for energy and momentum were manually selected to preserve the central events around $E_{tot} = \SI{17.76}{MeV}$. \\

\begin{figure}[bt]
	\centering
	\begin{minipage}{.33\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/0+/true_events/cuts_raw.pdf}
	\end{minipage}%
	\begin{minipage}{.33\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/0+/true_events/cuts_tdc.pdf}
	\end{minipage}%
	\begin{minipage}{.33\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/0+/true_events/cuts_Ep.pdf}
	\end{minipage}
	\caption{The effects of the three cuts. The left panel shows the raw data, without any cuts. The middle panel shows the same data after the \TDC{} cut has been applied. The right panel is a zoomed-in version of the middle panel, where the  vertical and horizontal lines illustrates the energy and momentum cut, respectively. Note the different column scale in each panel. }
	\label{fig: analysis: energy momentum cuts}
\end{figure}

With all our cuts imposed on the data, we can finally continue with the data analysis. 

\subsection{Dalitz plots}
The purpose of this thesis is to test the validity of the currently established models for the $0^+$ state. Thus a nice opener to the analysis is to make a Dalitz plot of the data. This plot can be seen on the left panel of figure \ref{fig: analysis: dalitz: slice}, which reveals that another cut is necessary. The figure shows that a large concentration of events are located in a band near $y \simeq 1$, where almost the entire energy of the first decay is given to the primary $\alpha$-particle. These events thus corresponds to decays through the ground state of \ce{^8Be}, i.e. through the $\alpha_0$ channel. A simple way of getting rid of these $\alpha_0$ events would be to impose a cut on the y-coordinate, as done in \cite{Kuhlwein}. This cut is shown in the middle panel of the same figure. We can do even better than that, however, if we look at the momentum vectors instead of the energies. \\

\begin{figure}[tb]
	\centering
	\begin{minipage}{.33\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/0+/true_events/dalitz_slice.pdf}
	\end{minipage}%
	\begin{minipage}{.33\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/0+/true_events/dalitz_slice_cut.pdf}
	\end{minipage}%
	\begin{minipage}{.33\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/0+/true_events/dalitz_slice_E23_cut.pdf}
	\end{minipage}
	\caption{The cuts on the Dalitz plot. The left panel shows the data before any cuts, with the red lines signifying the cutoffs. The middle panel shows the same data after the simple y-coordinate cut. The right panel also shows the same data, but with a cut based on the $E_{2-3}$ energies.}
	\label{fig: analysis: dalitz: slice}
\end{figure}

We can use the momentum vectors of each event to calculate $E_{2-3}$ from (\ref{theory: kinematics: E23}). The results of this can be seen on figure \ref{fig: analysis: dalitz: E23}. Here we clearly see the two peaks corresponding to the $\alpha_0$ and $\alpha_1$ channels, located at the correct energies - note the split in the $x$ axis. By now performing a simple Gaussian fit of the right peak and discarding any event outside $3\sigma$, we can extract only the excited decay events, which can be seen on the right panel of the Dalitz slice panels, figure \ref{fig: analysis: dalitz: slice}. This method is even more aggressive than the simple $y$ coordinate cut, and removes quite a lot of events - see table \note{create table}. Since it is based on the $E_{2-3}$ energies, we are confident that only irrelevant $\alpha_0$ events are removed. \\

\begin{figure}
	\centering
		\begin{minipage}{.5\textwidth}
		\centering
		\includegraphics[width=\textwidth]{figures/0+/true_events/E23.pdf}
	\end{minipage}%
	\begin{minipage}{.5\textwidth}
		\centering
		\includegraphics[width=\textwidth]{figures/0+/reconstructed_events/E23.pdf}
	\end{minipage}
	\caption{The $E_{2-3}$ values from (\ref{theory: kinematics: E23}). The left panel shows the plots for our multiplicity three data, while the right shows it for the multiplicity two data. The left peak corresponds to ground state decays, while the right peak is for excited decays. Note the difference in both the $x$ and $y$ axes for the two peaks. }
	\label{fig: analysis: dalitz: E23}
\end{figure}

Now that we have imposed all of the necessary cuts, we are ready to start the analysis of the plot itself. Although all of the kinematic information is already stored in a single slice, it is somewhat easier to see the structure in a standard Dali´tz plot. We can reconstruct it by repeatedly mirroring the slice along one of its edges. By doing this, we obtain figure \ref{fig: analysis: dalitz: full}. The left panel shows the multiplicity three data, while the right panel shows the multiplicity two data. The general structure is clearly seen to be similar to an inverted equilateral triangle, with somewhat bent sides in the right panel. Although they both share this structure, there are also a series of important differences. Firstly, the three vertices do not extend all the way out to the edge in the left panel, while they do in the right. Secondly, the left panel has \textit{zero} events in its very center, while the right still has a few. Thirdly, we see some weird cone-shaped areas at the very top of the right panel. These cones are clearly unnatural, and probably originates from reconstructed events with random proton coincidences which has managed to survive our cuts. To see why this is the cause, the raw Dalitz plots without any cuts can be found in the appendix, figure \ref{fig: appendix: 0+ dalitz uncut}. Here the cones extends outside the inscribed circular area, meaning they do not conserve energy - a very clear indicator that they contain protons. \note{why the difference at the very edge? and center? ask hans} \\

\begin{figure}[]
	\centering
	\begin{minipage}{.5\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/0+/true_events/dalitz.pdf}
	\end{minipage}%
	\begin{minipage}{.5\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/0+/reconstructed_events/dalitz.pdf}
	\end{minipage}
	\caption{The Dalitz plots for the data. The left panel shows the plot for our multiplicity three data, while the right shows it for the multiplicity two data. Note the large difference in the column scale.}
	\label{fig: analysis: dalitz: full}
\end{figure}

Our next goal is to figure out what we expected to see. Although Zemach's analysis does not impose any constraints upon a $0^+$ resonance, it is curious that the center appears to be forbidden. To get further than this, we will have to perform simulations of the decay. In section \ref{section: data reduction} we discussed two possible simulation tools, \simX{} and \simta{}, both based on R-matrix theory. Only the latter accounts for our three identical bosons and the interference between them, and it is thus our preferred choice. \\

The result of a \simta{} simulation can be seen on the left panel of figure \ref{fig: analysis: dalitz: sim}. Something is clearly very wrong here: it does not at all look like our data. More specifically, there is no evidence of an inverted equilateral triangle, and the center is not at all suppressed. As a quick check, another simulation was performed solely with the \simX{}, which results in the right panel of the same figure. While this is a far better match to our data, the edges around the vertices still look very different. 

\begin{figure}[]
	\centering
	\begin{minipage}{.5\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/0+/sim/dalitz_sim3a.pdf}
	\end{minipage}%
	\begin{minipage}{.5\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/0+/sim/dalitz_simX.pdf}
	\end{minipage}
	\caption{The Dalitz plots for the $0^+$ simulations. The left panel shows the results of the \simta{} simulation, while the right shows it for the \simX{} simulation. For a discussion on the difference between the two tools, see section \note{ref}.}
	\label{fig: analysis: dalitz: sim}
\end{figure}

\subsubsection{Checking the simulation tools}
Since the simulations are so different from what we see in our data, it is worthwhile to verify that they actually work as expected. Since we also have data on the $2^-$ resonance which has been the subject of several papers (see e.g. \cite{Kuhlwein}), it is an obvious target for checking the tools. \\

Using the custom Dalitz fitter introduced in section \ref{sec: datared: custom tools} to fit all our $2^-$ data (both multiplicity $2$ and $3$) with a \simta{} simulation for both $2^-_1$ and $2^-_3$, we get figure \ref{fig: analysis: dalitz: 2-}, which we can already at a glance tell is a far better match than the $0^+$ resonance. The reason the simulation appears "grainy" is due to a low number of simulated events, which cannot be increased any further due to time constraints. The fit already takes an hour to perform, and, if we are optimistic, scales linearly with the number of events. Since we will need quite a few more of these fits later, the simulated number of events was limited to those listed in table \note{make table}. \\

\begin{figure}[]
	\centering
	\includegraphics[width=\linewidth,height=0.5\linewidth]{figures/2-/sim/true_events 2-_i/dalitz.pdf}
	\caption{The Dalitz plots for the $2^-$ resonance. The left panel shows both the multiplicity two and three data, while the right shows a fit to the \simta{} simulation.}
	\label{fig: analysis: dalitz: 2-}
\end{figure}

Visually, this fit is excellent, but since it is rather hard to compare 2D histograms by eye, it is common to look at their projections instead. A popular choice are the polar projections, along with the center-of-mass energies. If we plot these, we get the three panels shown on figure \ref{fig: analysis: dalitz: 2- projections}. Here the black lines represents the data, while the orange represents the simulation. Although there are some deviations, especially for the radial projection $\rho$, this is overall a great fit to the data. For $\chi^2$ and fitted values, see table \ref{table: analysis: dalitz fit results}. \\

\begin{figure}[bt]
	\centering
	\begin{minipage}{.33\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/2-/sim/true_events 2-_i/E_cm.pdf}
	\end{minipage}%
	\begin{minipage}{.33\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/2-/sim/true_events 2-_i/rho.pdf}
	\end{minipage}%
	\begin{minipage}{.33\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/2-/sim/true_events 2-_i/phi.pdf}
	\end{minipage}
	\caption{The projections of the two $2-$ Dalitz plots from figure \ref{fig: analysis: dalitz: 2-}. The black lines represents the data, while the orange lines are the simulation. The two lines match nicely in all of the projections.}
	\label{fig: analysis: dalitz: 2- projections}
\end{figure}

As mentioned previously, this is the exact same $2^-$ data as that used in \cite{Kuhlwein}. Although there are some differences in the techniques used to filter the data, the results are very similar, as can be seen in table \ref{table: analysis: dalitz: this and kuhlwein}. Note that to make a direct comparison, what is listed there is for the fit using both multiplicity $2$ and $3$ data. The other entries in the table will be relevant in a later discussion. \\

\begin{table}[h]
	\begin{tabular}{l|p{0.35\textwidth}p{0.4\textwidth}}
		& This paper & Kuhlwein \cite{Kuhlwein} \\ \hline
		\vrule width 0pt height 2.5ex
		$\text{abs}(\Delta E)$ & \SI{300}{keV} & \SI{200}{keV} \\
		$p_{tot}$ & \SI{50}{MeV} & \SI{35}{MeV} \\
		$\alpha_0$ removal & $E_{2-3}$ fit & Dalitz $y$-coordinate cut \\
		\TDC{} cut & \TDC{} difference for individual hits & \TDC{} difference for decay events \\
		Algorithm & Genetic & Migrad \\ \hline
		\vrule width 0pt height 2.5ex
		$k$ & 20.9(3)\% & 20.5(2)\% \\
		$\delta\ [2\pi]$ & 67.82(8)\% & 67.10(2)\% \\
		$\chi^2$ & 16 205 & 14 028 \\
		dof & 5346 & $\sim$5700
	\end{tabular}
	\caption{The main differences in the analysis performed here and in \cite{Kuhlwein}. We can obtain a slightly better fit by using only multiplicity $3$ data, see table \note{ref}. Kuhlwein can do even better than that by also accounting for Coulomb interaction effects; the numbers listed here are only for comparison. \note{bins or dof}}
	\label{table: analysis: dalitz: this and kuhlwein}
\end{table}

With an assurance that the simulations are not wrong, we can instead turn our attention to the R-matrix parameters they use. Kuhlwein found that for the $2^-$ state, the optimal values were generally not in agreement with prior research, and it is thus very likely that the same would apply to the $0^+$ state. Thus it would be interesting to look at what the effects of changing this parameter is.

\subsubsection{Checking the reduced width amplitude}
A solution to our problem could be that the reduced width amplitude used for the first excited state of \ce{^8Be^*} is wrong. We have consistently been using the value $\gamma = \SI{32.787}{\sqrt{keV}}$ as found by \cite{Bhattacharya}. While we examine the effects of changing this value, we can simultaneously use it as an additional check on our two simulation tools: as the width becomes smaller, interference effects becomes less relevant, and so the two simulations should converge towards each other for low $\gamma$. \\

As we discussed in section \ref{section: theory} of the theory, in a consecutive three-body breakup, the angle between the primary and secondary particles are correlated. We also derived the correlation function $W(\beta)$ (\ref{theory: angular correlations: W}) for an arbitrary state. Combined with the equation for $\alpha = \pi - \beta$ (\ref{theory: kinematics: theta}), we can use this angle to compare the angular correlation implicit in the simulation with what we would expect for a $0^+$ resonance. The angle $\alpha$ depends only on the Dalitz coordinates $x$ and $y$, meaning we can easily extract it from a Dalitz plot by projecting down onto the $x$ axis. Since it depends also on the $y$ coordinate, we must limit the projection to a small area around an interesting area. With these simulations, that area is the main horizontal band, which can be seen on figure \ref{fig: analysis: ang compare 0} for both \simta{} and \simX{}. \\

\begin{figure}[h]
	\includegraphics[width=\linewidth]{figures/0+/sim/ang_compare_0_sim3a.pdf}
	\includegraphics[width=\linewidth]{figures/0+/sim/ang_compare_0_simX.pdf}
	\caption{ang compare}
	\label{fig: analysis: ang compare 0}
\end{figure}

This figure consists of two separate plots: the top two rows shows the output of \simta{}, while the bottom two shows \simX{}. The reduced amplitude width $\gamma$ used for each plot is used as the $x$ label of the top panels, while the corresponding formal width $\Gamma$ is used as the label for the bottom panels. This plot is mainly for illustrating the relationship between the Dalitz plots and the correlation angle. Since we will be analyzing these two parts separately, figures \ref{fig: analysis: width effect dalitz all} and \ref{fig: appendix: ang compare all} in the appendix shows them individually. 

\begin{figure}[H]
	\centering
	\includegraphics[width=\linewidth]{figures/0+/sim/width_compare_0.pdf}
	\includegraphics[width=\linewidth]{figures/0+/sim/width_compare_1.pdf}
	\includegraphics[width=\linewidth]{figures/0+/sim/width_compare_2.pdf}
	\caption{\note{describe me}}
	\label{fig: analysis: width effect dalitz all}
\end{figure}

Let us first focus on the structure of the Dalitz plots. When looking at figure \ref{fig: analysis: width effect dalitz all}, the effects of changing $\gamma$ becomes very apparent. As mentioned earlier, the value used for the previous Dalitz plot, figure \ref{fig: analysis: dalitz: sim}, was $\gamma = \SI{32.787}{\sqrt{keV}}$, where only the three vertices and an abundance of events in the very center could be seen. We now see that both of these areas are the results of a large amount of interference between the connecting lines \note{better word?}: starting with no interference in the top left panel, it gradually becomes more apparent until it is the only remaining feature for $\gamma = 40$. Similarly we also see an increasing amount of interference as the lines broaden on the \simX{} panels, although no destructive interference can be seen there. Even when changing this width, none of the panels are a good match for our data. This would imply that the data may not be from a $0^+$ at all, which we will explore later. \\

The other figure, \ref{fig: appendix: ang compare all} in the appendix, shows the extracted correlation angle from each of the Dalitz plots of figure \ref{fig: analysis: width effect dalitz all}. The agreement between theory and simulation for \simta{} is generally quite good, except at the points of maximum interference. \simX{} is not quite as good, especially around the points of interference, and generally appears to be more smeared. The general shape is clearly seen in both figures, indicating that they work as expected. This also provides further evidence to the previous statement: the $0^+$ correlation function predicts the "holes" we see on the simulated \simta{} Dalitz plots, but which were not present in the data. This would then imply that the data does not follow the expected angular correlation distribution - we will take a closer look at this in the next section. Finally, the two tools does not really appear to be converging as $\gamma$ decreases. It appears that this is mainly due to the \simX{} simulations not being suppressed near the "holes", as they should. Why this is the case is unknown \note{ask Hans}. \\

As promised, the next step is to take a closer look at the distribution of the correlation angle of the data, since it does not appear to follow the expected distribution for a $0^+$ resonance. 

\subsubsection{Checking the angular correlations}
We have two options for extracting the correlation angle from our data. The first option is to use the same method as in the previous section, where we extract the correlation angle $\alpha$ directly from the data. For the same reasons as was discussed there, a small area surrounding the main horizontal band was chosen, as can be seen on the top panels of figure \ref{fig: analysis: ang cor alpha fit}. The projection itself is shown on both the bottom panels and on figure \ref{fig: analysis: ang proj corr funcs}, where we will first focus on the latter. \\

\begin{wrapfigure}[18]{r}{7cm}
	\vspace{-16pt}
	\centering
	\includegraphics[width=\linewidth]{figures/0+/true_events/ang_cor_fit.pdf}
	\caption{The correlation angle extracted from the data along with all relevant theoretical correlation functions. The $0^+$ function is a terrible fit to the data. }
	\label{fig: analysis: ang proj corr funcs}
\end{wrapfigure}

On figure \ref{fig: analysis: ang proj corr funcs} the projection of the band along with all correlation functions up to and including the $J = 3$ states are shown. The idea with this figure is that we can visually check which correlation functions could potentially match our data. As we also discovered in the previous section, the shape of the $0^+$ function is completely wrong, and could never possibly explain the data. Similarly we can rule out all of the states which peaks at $x = \pm 1$. We are then left with the options $\{1^+_2, 2^-_1, 2^-_3, 3^+_2\}$. Neither of these is a good match by themselves, so we need a linear combination of them. Both $2^-_1$ and $1^+_2$ "undershoots" their prediction, and would need to be partnered with one or both of $2^-_3$ and $3^+_2$, which "overshoots". Since $1^+_2$ and $3^+_2$ would both need to be partnered with another state, using those would imply our data is dominated by not only a single other state, but \textit{two} others. Physically it would thus make better sense if we allow only a linear combination of the two $2^-$ transitions. \\

Thus a fit with these two were made, which can be seen on the bottom panels of figure \ref{fig: analysis: ang cor alpha fit}. An arbitrary scaling factor was also allowed as a free fitting variable, since a linear combination of the correlation functions is not necessarily normalized. We see that the fit is in remarkably good agreement with our multiplicity $3$ data, except at the points of maximum interference. Similarly with the multiplicity $2$ data, where it appears to be even better. Overall it is rather surprising that a simple fit of just two $2^-$ transitions is in such good agreement with the data. After this first fit was made, a second fit which also allows for a $0^+_2$ component was made. In both cases the ratio of this state was vanishingly low. The fit values can be seen in table \ref{table: analysis: ang cor fit result}. \\

\begin{figure}[h]
	\centering
	\begin{minipage}{.5\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/0+/true_events/ang_cor_fit_cut.pdf}
	\end{minipage}%
	\begin{minipage}{.5\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/0+/reconstructed_events/ang_cor_fit_cut.pdf}
	\end{minipage}
	\begin{minipage}{.5\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/0+/true_events/ang_2-_fit.pdf}
	\end{minipage}%
	\begin{minipage}{.5\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/0+/reconstructed_events/ang_2-_fit.pdf}
	\end{minipage}
	\caption{}
	\label{fig: analysis: ang cor alpha fit}
\end{figure}

Instead of hand-picking a specific area to project onto the $x$ axis, we can extract the angle $\beta$ from the momentum vectors, as described by (\ref{theory: kinematics: theta data}). These two angles are simply related by $\alpha = \pi - \beta$, as can be seen on figure \ref{fig: theory: kinematics: angles}. We can then repeat the analysis we made for the other angle, except we do not have to restrict ourselves to a small area, but can instead use all of the available data. \\

\begin{figure}[h]
	\centering
	\begin{minipage}{.35\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/0+/true_events/breakup_angle_data.pdf}
	\end{minipage}%
	\begin{minipage}{.35\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/0+/reconstructed_events/breakup_angle_data.pdf}
	\end{minipage}
	\begin{minipage}{.35\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/0+/true_events/breakup_angle_2-_0+_fit.pdf}
	\end{minipage}%
	\begin{minipage}{.35\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/0+/reconstructed_events/breakup_angle_2-_0+_fit.pdf}
	\end{minipage}
	\caption{}
	\label{fig: analysis: ang cor beta fit}
\end{figure}

The results of doing this can be seen on figure \ref{fig: analysis: ang cor beta fit}, where the top panels shows the correlation angle for both $\alpha_0$ and $\alpha_1$ channel decays. The bottom panels then shows the actual fit, the results of which can be found in table \ref{table: analysis: ang cor fit result}. The multiplicity $3$ fit roughly agrees with what we found through the $\alpha$ fit. The multiplicity $2$ fit is far more interesting: it allows a substantial $0^+$ component, and also appears to be the best fit we have seen so far. Graphically this makes sense since the $2^-$ functions both goes to zero at $x=-1$, which the $0^+$ function must then make up for. The reason for this large difference between the two sets of data is unclear. Based on the earlier discussion on the differences between them \note{actually make this}, the multiplicity $3$ data should be the most trustworthy. \\

\begin{table}[h]
	\centering
	\begin{tabular}{ccccc}
		Correlation angle & Multiplicity & $k_{2^-_1}$ & $k_{2^-_3}$ & $k_{0^+_2}$ \\ \hline
		$\alpha$ & 3 & 0.34(15) & 0.66(15) & 0(1e-13) \\
		$\alpha$ & 2 & 0.18(12) & 0.82(12) & 0(3e-11) \\
		$\beta$  & 3 & 0.52(24) & 0.48(24) & 0(0.13) \\
		$\beta$  & 2 & 0.14(22) & 0.48(22)& 0.38(20)
	\end{tabular}
	\caption{table}
	\label{table: analysis: ang cor fit result}
\end{table}

Now that we have an idea of what states we are looking for, we can use the Dalitz fitting tool introduced in section \ref{sec: datared: custom tools} to try to fit the data to simulations of them. \note{aren't 0+ decays supposed to be uncorrelated?}

\subsection{Fitting the data}
All fit results from this section can be found in table \ref{table: datared: dalitz fitter}. \\

\subsubsection{$0^+$}
Based on the results of the previous section, it appears as if our data is dominated by a $2^-$ state, possibly with a small component of $0^+$. With this knowledge, we can attempt to fit the data with such a state. By firing up the custom Dalitz fitter with the $0^+$ data and a $2^-$ simulation, we get the result seen on the top row of figure \ref{fig: analysis: dalitz fit 0+}. \\

\begin{figure}[H]
	\centering
	\begin{minipage}{.33\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/0+/sim/true_events 2-_i/E_cm.pdf}
	\end{minipage}%
	\begin{minipage}{.33\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/0+/sim/true_events 2-_i/rho.pdf}
	\end{minipage}%
	\begin{minipage}{.33\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/0+/sim/true_events 2-_i/phi.pdf}
	\end{minipage}
	\begin{minipage}{.33\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/0+/sim/true_events 2-_i 3-_i/E_cm.pdf}
	\end{minipage}%
	\begin{minipage}{.33\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/0+/sim/true_events 2-_i 3-_i/rho.pdf}
	\end{minipage}%
	\begin{minipage}{.33\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/0+/sim/true_events 2-_i 3-_i/phi.pdf}
	\end{minipage}
	\begin{minipage}{.5\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/0+/sim/true_events 2-_i/binwise_chi2.pdf}
	\end{minipage}%
	\begin{minipage}{.5\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/0+/sim/true_events 2-_i 3-_i/binwise_chi2.pdf}
	\end{minipage}
	\caption{The various projections of the $\{2^-_1, 2^-_3\}$ (top row) and $\{3^-_1, 3^-_3\}$ (middle row) fits. The bottom row shows the binwise $\chi^2$ contributions, where the left belongs to the top row and the right belongs to the middle row. For the fitted values, see table \ref{table: analysis: dalitz fit results}.}
	\label{fig: analysis: dalitz fit 0+}
\end{figure}

Overall this fit is decent, but by no means perfect, which would indicate the presence of another component. It would make sense if this was the missing $0^+$ component, so a fit allowing for this was made. Mirroring the results of the angular correlation fits, the fit found a vanishingly small component of $0^+$. In an attempt to figure out what else could contribute, a series of fits were made allowing for a $3^-$ component, which can be seen in the same figure on the middle row. As can be seen in table \ref{table: analysis: dalitz fit results}, this fit is slightly better, and results in a mix of about 4:1 of $2^-$ to $3^-$. If we look at the binwise $\chi^2$ contribution (bottom row of figure \ref{fig: analysis: dalitz fit 0+}), we see that this final fit is almost perfect, except for a major deviation at the very edge. If we look at the output Dalitz plots themselves, we can see that the reason for this difference is that in the data there are \textit{no} events located at the very edge - it appears as if a cut has been made. Neither the $2^-$ nor $3^-$ states shares this peculiarity, see figure \note{make fig} in the appendix.  \note{comment on this later - this combined with the relatively high chi2 values compared to 2- and 3- could mean that another state might be a better match, although that seems unlikely based on the previous analysis} \\

To avoid the weird structure arising from random proton coincidences (see figure \ref{fig: analysis: dalitz: full}), an aggressive cut of $y = 0.85$ is made when using multiplicity $2$ data. The same cut will be imposed on the $3^-$ data, which suffers from the same abnormalities. Due to the lower beam energy, no such issues are present in the $2^-$ data, which is left untouched. 

\subsubsection{$2^-$}
We have already briefly talked about the $2^-$ fits, which will be expanded upon here. Figure \ref{fig: analysis: dalitz: 2- projections} shows its projections, which are all extremely good matches. \\

As was also mentioned earlier, Kuhlwein \cite{Kuhlwein} looked at the exact same data, and found very similar results, as can be seen in table \ref{table: analysis: dalitz: this and kuhlwein}. Since Kuhlwein did not perform separate analyses on the two multiplicities of the data, the result listed in that table is for a fit using both multiplicity $2$ and $3$ data. We can do even better than that by only using the latter, as can be seen in table \ref{table: analysis: dalitz fit results}. To get even better results, we should account for the Coulomb interaction between the primary and secondary particles, which is neglected in (\ref{datared: tools: balamuth eq}). One could account for this simply by introducing extra barrier penetrabilities in the equation, one for each of the two secondary particles \cite{Fynbo}. \note{could this be the edge effect?} \\

\begin{figure}[h]
	\centering
	\begin{minipage}{.5\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/2-/sim/true_events 2-_i/binwise_chi2.pdf}
	\end{minipage}%
	\begin{minipage}{.5\textwidth}
		\centering
		\includegraphics[width=\linewidth]{figures/2-/sim/true_events 2-_i/binwise_chi2.pdf}
\end{minipage}
	\caption{text}
	\label{fig: analysis: 2- dalitz fit chi2}
\end{figure}

Kuhlweins analysis accounts for this Coulomb repulsion, which further improves their fits to a a $\chi^2$ value of just $9165$. Compare this with our best estimates in table \ref{table: analysis: dalitz fit results}. Based on Kuhlweins binwise $\chi^2$ plots, it appears as if this correction improves mainly the areas near the edges of the Dalitz plot, which is exactly the area all our fits are struggling with, see figures \ref{fig: analysis: dalitz fit 0+} and \ref{fig: analysis: 2- dalitz fit chi2} for our binwise $\chi^2$ plots. Thus it is expected that this correction would dramatically improve our fits. However, due to time constraints, this correction could not be implemented. \note{ask Hans}

\subsubsection{$3^-$}
Finally, since we have the data available anyway, a fit was made to the $3^-$ data as well. The goodness-of-fit is similar to that of the $2^-$ state, as can be seen in table \ref{table: analysis: dalitz fit results}. The resulting projections and binwise $\chi^2$ values can be found in figure 

\begin{landscape}
\begin{table}[]
	\begin{tabular}{cccccccccc}
		Data & Multiplicity & Simulation 1 & Simulation 2 & $\chi^2$ & $k_1$ & $\delta_1\ [2\pi]$ & c & $k_2$ & $\delta_2\ [2\pi]$ \\ \hline
		$0^+$ & 3 & $2^-_1, 2^-_3$ &  & 29 561 & 7.86(2)\% & 66.9(1)\% &  &  &  \\
		& 3 & $2^-_1, 2^-_3$ & $0^+_2$ & 29 516 & 7.87(2)\% & 66.9(1)\% & 1 (1e-5) &  &  \\
		& 3 & $2^-_1, 2^-_3$ & $3^-_1$ & 26 823 & 8.95(3)\% & 66.3(1)\% & 0.916(2) &  &  \\
		& 3 & $2^-_1, 2^-_3$ & $3^-_3$ & 29 381 & 7.26(2)\% & 67.3(2)\% & 0.986(1) &  &  \\
		& 3 & $2^-_1, 2^-_3$ & $3^-_1, 3^-_3$ & 25 230 & 22.0(6)\% & 66.2(1)\% & 0.811(4) & 37(1)\% & 27.6(3)\% \\
		& both & $2^-_1, 2^-_3$ &  & 111 408 & 90.9(1)\% & 8.22(8)\% &  &  &  \\
		& both & $2^-_1, 2^-_3$ & $0^+_2$ & 31 049 & 50\% & 63.3\% & 0.771 &  &  \\
		& both & $2^-_1, 2^-_3$ & $3^-_1$ & 36 263 & 99.3\% & 12.4\% & 0.703 &  &  \\
		& both & $2^-_1, 2^-_3$ & $3^-_3$ & 37 344 & 78.6(4)\% & 83.1(2)\% & 0.574(2) &  &  \\
		& both & $2^-_1, 2^-_3$ & $3^-_1, 3^-_3$ & 39 332 & 97.4\% & 1.81\% & 0.709 & 56.8\% & 9.26\% \\ \hline
		$2^-$ & 3 & $2^-_1, 2^-_3$ &  & 13 453 & 19.8(5)\% & 67.4(2)\% &  &  &  \\
		& 3 & $2^-_1, 2^-_3$ &  & 15 951 & 14.0(5)\% & 0 (fixed) &  &  &  \\
		& both & $2^-_1, 2^-_3$ &  & 16 205 & 20.9(3)\% & 67.82(8)\% &  &  &  \\
		& both & $2^-_1, 2^-_3$ &  & 24 075 & 15.0(3)\% & 0 (fixed) &  &  &  \\ \hline
		$3^-$ & 3 & $3^-_1, 3^-_3$ &  & 13 691 & 11.3(6)\% & 62.1(3)\% &  &  &  \\
		& 3 & $3^-_1, 3^-_3$ & $2^-_1$ & 12 314 & 6.81(7)\% & 65.9(7)\% & 0.90(1) &  &  \\
		& 3 & $3^-_1, 3^-_3$ & $2^-_3$ & 12 375 & 10.0(6)\% & 63.6(3)\% & 0.865(6) &  &  \\
		& 3 & $3^-_1, 3^-_3$ & $2^-_1, 2^-_3$ & 13 180 & 15.1\% & 63.0\% & 0.886 & 92.5\% & 75.9\% \\
		& 3 & $3^-_1, 3^-_3$ & $1^+_2$ & 15 915 & 32(1)\% & 3.7(3)\% & 0.613(3) &  &  \\
		& both & $3^-_1, 3^-_3$ &  & 21 826 & 6.3(4)\% & 57.9(3)\% &  &  &  \\
		& both & $3^-_1, 3^-_3$ & $2^-_1$ & 18 961 & 9.6(4)\% & 61.4(2)\% & 1(1e-4) &  &  \\
		& both & $3^-_1, 3^-_3$ & $2^-_3$ & 18 901 & 6.7(4)\% & 64.2(2)\% & 0.804(4) &  &  \\
		& both & $3^-_1, 3^-_3$ & $2^-_1, 2^-_3$ & 20 112 & 15.1\% & 61.4\% & 0.848 & 87.9\% & 82.9\% \\
		& both & $3^-_1, 3^-_3$ & $1^+_2$ & 15 986 & 2.7(2)\% & 26.4(8)\% & 0.884(2) &  & 
	\end{tabular}
	\caption{The results of various fits performed with the Dalitz fitting tool. \note{1+ invalid}}
	\label{table: analysis: dalitz fit results}
\end{table}
\end{landscape}

\subsection{Some title}
\subsubsection{Verifying the branching ratio}
\begin{table}[h]
	\begin{tabular}{l|lllll}
		Data & $\alpha_0$ hits & $\alpha_0$ efficiency & $\alpha_1$ hits & $\alpha_1$ efficiency & Branching ratio \\ \hline
		$0^+$ & 260 828 &  & 524 813 &  & 0.155 \\
		$3^-$ & 87 225 &  & 96 566 &  & 0.429
	\end{tabular}
\end{table}